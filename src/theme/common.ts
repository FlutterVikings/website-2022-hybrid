import config from '../config';

const transitions = {
  normal: '0.5s',
};

const fontSize = {
  small: '1rem',
  base: '1.3rem',
  medium: '1.8rem',
  big: '2.9rem',
  xxl: '8rem',
  xxxl: '13rem',
};

const fontFamily = {
  headerFontFamily: config.headerFontFamily,
  bodyFontFamily: config.headerFontFamily,
};

export { transitions, fontSize, fontFamily };
